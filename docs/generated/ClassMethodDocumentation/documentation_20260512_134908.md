# Technical Documentation

## File: `testClass.cs`

### Overview
This file defines a namespace `src` containing a single class `testClass`. The `testClass` class is a simple data container with two properties, `name` and `age`, and three methods for printing information to the console.

---

### Namespace: `src`

---

### Class: `testClass`
The `testClass` class models a simple entity with the following data members and methods:

#### Properties
- **`string name`**  
  Public property that stores the name of the entity.

- **`int age`**  
  Public property that stores the age of the entity.

#### Methods

- **`void printInfo()`**  
  Prints the current values of `name` and `age` to the console in the format:
  ```csharp
  Name: {name}, Age: {age}
  ```
  *Parameters:* None  
  *Return:* None

- **`void printAnotherThing()`**  
  Prints the fixed message:
  ```
  This is another thing.
  ```
  *Parameters:* None  
  *Return:* None

- **`void printYetAnotherThing()`**  
  Prints the fixed message:
  ```
  This is yet another thing.
  ```
  *Parameters:* None  
  *Return:* None

---

### Example usage

```csharp
using src;

class Program
{
    static void Main()
    {
        testClass obj = new testClass();
        obj.name = "Alice";
        obj.age = 30;

        obj.printInfo();                 // Output: Name: Alice, Age: 30
        obj.printAnotherThing();         // Output: This is another thing.
        obj.printYetAnotherThing();      // Output: This is yet another thing.
    }
}
```

---

### Summary
The `testClass` provides a straightforward implementation demonstrating basic class properties and console output methods. It can be utilized as a simple data holder and utility for printing preset and dynamic information.