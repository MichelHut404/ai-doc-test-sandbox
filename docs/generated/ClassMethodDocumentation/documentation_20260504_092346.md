# Documentation for `testClass.cs`

## Overview
The `testClass.cs` file is part of the `src` namespace and defines a simple class `testClass` which models an entity with a name and age. It includes functionality to print these properties to the console.

## Classes

### `testClass`
This is a basic data container class with two public properties: `name` and `age`. It provides a method to output these properties to the console.

#### Properties

- `string name`  
  Gets or sets the name associated with the object.

- `int age`  
  Gets or sets the age associated with the object.

#### Methods

##### `void printInfo()`
Prints the current values of `name` and `age` to the console in a human-readable format.

**Parameters:**  
None.

**Return value:**  
None.

**Example usage:**
```csharp
var person = new testClass();
person.name = "John";
person.age = 30;
person.printInfo();  // Output: Name: John, Age: 30
```